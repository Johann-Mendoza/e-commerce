<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Product Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" rel="stylesheet">
    <!-- Custom Styles -->
    <style>
        body {
            background-color: #f8f9fa; /* Light gray background */
            font-family: 'Arial', sans-serif;
        }
        .container {
            margin-top: 50px;
            max-width: 800px;
            background-color: #ffffff; /* White background for the container */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        h1 {
            font-size: 2.5rem;
            color: #8b4513; /* Saddle brown for the main title */
            text-align: center;
            margin-bottom: 20px;
        }
        h2 {
            color: #d2b48c; /* Khaki for the price */
            font-weight: 700;
            text-align: center;
            margin-bottom: 20px;
        }
        img {
            display: block;
            margin: 0 auto;
            max-width: 100%;
            height: auto;
            border-radius: 8px;
            box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
        }
        p {
            font-size: 1.1rem;
            color: #6c757d; /* Dark gray for text */
            margin-top: 20px;
            text-align: justify;
        }
        .btn {
            display: block;
            width: 100%;
            margin: 20px 0;
            padding: 10px 0;
            font-size: 1.2rem;
        }
        .btn-primary {
            background-color: #d2b48c; /* Khaki for the primary button */
            border: none;
            transition: background-color 0.3s ease;
        }
        .btn-primary:hover {
            background-color: #c2a97c; /* Darker khaki on hover */
        }
        .btn-secondary {
            background-color: #6c757d; /* Dark gray for the secondary button */
            border: none;
            color: white; /* White text for better contrast */
        }
        .btn-secondary:hover {
            background-color: #5a6268; /* Darker gray on hover */
        }
    </style>
</head>
<body>

    <div class="container">
        <h1><%= product.name %></h1>
        <img src="<%= product.image_url %>" alt="<%= product.name %>">
        <h2>Price: $<%= product.price.toFixed(2) %></h2>
        <p><%= product.description %></p> <!-- Assuming you have a description field -->
        
        <form action="/cart/add" method="POST">
            <input type="hidden" name="productId" value="<%= product.id %>">
            <button type="submit" class="btn btn-primary">Add to Cart</button>
        </form>
        <a href="/dashboard" class="btn btn-secondary">Back to Dashboard</a>
    </div>

</body>
</html>
